import { useSelector } from "react-redux";
import { useParams } from "react-router-dom";


function ProductPage() {
    let params = useParams();
    const { id } =  params;


  return (
    // <div>{findProduct ? "нашли" : "не нашли"}</div>
    <div>id</div>
  )
}

export default ProductPage;